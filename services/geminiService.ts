import { GoogleGenAI } from "@google/genai";
import { Student } from "../types";

const ai = new GoogleGenAI({ apiKey: process.env.API_KEY });

export const generateStudentAnalysis = async (student: Student): Promise<string> => {
  try {
    const scoresSummary = student.scores
      .map(s => `- ${s.subjectName}: ${s.score}/${s.fullScore} (เกรด ${s.grade})`)
      .join("\n");

    // Determine teacher gender and name for persona customization
    const teacherName = student.teachers[0] || "";
    const isMale = teacherName.startsWith("นาย");
    const shortName = teacherName.split(' ')[0] || "ครู";
    
    const genderRole = isMale ? "ครูผู้ชาย" : "ครูผู้หญิง";
    const endingParticle = isMale ? "ครับ" : "ค่ะ";

    const prompt = `
      บทบาท: คุณคือ "${shortName}" ซึ่งเป็นครูประจำชั้นระดับประถมศึกษาที่เป็น${genderRole} มีความใจดี อบอุ่น และเข้าใจพัฒนาการเด็ก
      งานของคุณ: วิเคราะห์ผลการเรียนของนักเรียนชั้น ${student.class} ชื่อ "${student.firstName} ${student.lastName}" เพื่อสื่อสารกับผู้ปกครองและนักเรียน
      
      รูปแบบการพูด (Tone of Voice):
      - ต้องลงท้ายประโยคด้วยคำว่า "${endingParticle}" เสมอ เพื่อแสดงเอกลักษณ์ของ${genderRole}
      - แทนตัวเองว่า "ครู" หรือ "${shortName}"
      - ใช้น้ำเสียงอบอุ่น เป็นกันเอง ให้กำลังใจ เหมือนกำลังนั่งคุยกับนักเรียนและผู้ปกครองต่อหน้า
      - หลีกเลี่ยงภาษาทางการที่แข็งกระด้าง

      ข้อมูลผลการเรียน:
      ${scoresSummary}
      เกรดเฉลี่ย (GPA): ${student.gpa}

      กรุณาวิเคราะห์และให้คำแนะนำหัวข้อดังนี้ (ใช้ Emoji ประกอบให้น่าอ่าน):

      1. 🌟 **สิ่งที่หนูทำได้ดีเยี่ยม**: ชื่นชมวิชาที่คะแนนดี และทักษะที่โดดเด่นของนักเรียน
      2. 💡 **สิ่งที่พัฒนาได้อีก**: วิชาที่คะแนนน้อยกว่าวิชาอื่น (ถ้ามี) และแนะนำวิธีฝึกฝนง่ายๆ ที่ทำได้ที่บ้าน
      3. 📖 **คำแนะนำสำหรับผู้ปกครอง**: แนะนำวิธีส่งเสริมการเรียนรู้ให้ลูกอย่างมีความสุข
      4. 🌈 **ข้อความให้กำลังใจจาก${shortName}**: ข้อความสั้นๆ เพื่อสร้างความมั่นใจให้นักเรียนภูมิใจในตัวเอง

      หมายเหตุ: ไม่ต้องดุหรือตำหนิ เน้นการชมเชยและส่งเสริมพลังบวก (Positive Reinforcement)
    `;

    const response = await ai.models.generateContent({
      model: "gemini-2.5-flash",
      contents: prompt,
      config: {
        thinkingConfig: { thinkingBudget: 0 } 
      }
    });

    return response.text || "ขออภัย ระบบไม่สามารถสร้างคำแนะนำได้ในขณะนี้ โปรดลองใหม่อีกครั้ง";
  } catch (error) {
    console.error("Gemini API Error:", error);
    return "เกิดข้อขัดข้องในการเชื่อมต่อกับ AI ครูแนะแนว (กรุณาตรวจสอบการเชื่อมต่ออินเทอร์เน็ต)";
  }
};